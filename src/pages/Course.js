import React from 'react';
import Courses from '../components/Courses';

const Course = () => {
    return (
        <div>
            <Courses></Courses>
        </div>
    );
};

export default Course;